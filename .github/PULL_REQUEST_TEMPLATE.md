What changed and why, in a few sentences.

For verdicts and traps: the version you tested, and the sources with the date you read them. `pnpm validate:verdicts` passes.

For code: `pnpm typecheck` and `pnpm test` pass; new behaviour has a test.
